К этому времени вы уже достаточно знаете про типы векторов и без труда укажете иерархию среди четырёх основных типов, выстроив их в порядке от самого "узкого" к самому "широкому".

Логический (logical)
Целочисленный (integer)
Числа с плавающей точкой (numeric/double)
Строковый (character)

