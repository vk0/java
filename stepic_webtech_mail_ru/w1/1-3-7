На главной ﻿странице https://stepic.org/  используются различные CSS файлы.

Введите путь первого CSS файла, который загружается с домена stepic.org с помощью HTML.

Внимание: нужно ввести именно часть URL соответствующую пути, а не весь URL. ﻿Динамически загружаемые (с помощью JavaScript) ﻿стили - не учитывать.

/static/css/all.min.css