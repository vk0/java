Что из перечисленного справедливо в отношении операции sizeof?

Операция возвращает выраженный в байтах объем памяти, выделенной для объекта хранения (переменной).
Результат операции вычисляется с учетом выравнивания объекта.