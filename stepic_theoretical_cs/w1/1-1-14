Вам дан алгоритм отгадывания числа от 1 до 1000 делением пополам с ошибками в двух строках (в комментариях ошибок нет!).

Отметьте строки с ошибками:
 Верно!
 a <- 1, b <- 1001
 -- искомое число secret в интервале [a,b), то есть (1) больше или равно a; (2) меньше b.
 while (a < b) do
       m = (a + b) div 2; -- целая часть от полусуммы a и b (округление полуцелого вниз)
       if (secret > m) then
             a <- m
       else
             b <- m
       fi
 od
       print (a)


//7, 9