# Даны три переменные number1, number2, number3, в которых записаны целые числа.

# Проверьте, действительно ли сумма первых двух чисел больше, чем третье число, сохранив результат сравнения (TRUE или FALSE) в новую переменную и напечатав ее при помощи команды print() .

# Переменные number1, number2, number3 уже созданы, сразу приступайте к работе с ними.

# Sample Input:
# 0 6 0
# Sample Output:
# TRUE

c <- number1 + number2 > number3
print(c)