Вот алгоритм поиска максимального среди a[1],...,a[n], основанный на другой идее (см. инвариант в фигурных скобках), в котором одна из строк испорчена. Отметьте испорченную строку.

k<-1, i<-1
{ a[i]=max (a[1],...,a[k]) }
while k<=n do
  if a[k+1] >= a[i]
    then i<-k+1, k<-k+1
    else k<- k+1
  fi
od
print a[i]


line 5