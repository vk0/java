Синтаксис ассемблера для выполнения заданий

В отличие от приведенной в лекции нотации языка ассемблера (называемой нотацией Intel) в заданиях используется другая нотация (называемая нотацией AT&T), которую использует наша система проверки.

Основные отличия нотаций, которые надо знать для выполнения заданий в следующем:

наличие префикса % перед именем регистра (например, %eax, %ebx)
наличие префикса $$ перед константами (например, $$1, $0x333)
противоположный порядок операндов: сначала источник (откуда), потом приемник (куда) , например:
; в лекции

mov eax, ebx  ; пересылка из ebx в еах

mov eax, 1    ; пересылка значения 1 в регистр eax

; в упражнениях

mov %eax, %ebx  ; пересылка из еах в ebx

mov $1, %eax    ; пересылка значения 1 в регистр eax
Для выполнения заданий, этой информации должно быть вполне достаточно, кроме того, мы постарались в заданиях подробно написать про каждую команду, однако, если кому-то будут интересны дополнительные детали про AT&T синтаксис, то можно обратиться к следующей странице в википедии.s