Сопоставьте следующие строки кода с их словесными описаниями.

Изменяемый указатель на неизменяемую область памяти.
Неизменяемый указатель на изменяемую область памяти.
Неизменяемый указатель на неизменяемую область памяти.

const long *p1;
long *const p2;
const long *const p3;