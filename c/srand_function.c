#include <stdio.h>
#include <stdlib.h>
main () {
char i;
//int a, n;

//printf("Enter a integer: ");

//n = scanf("%d", &a);

for (i = 1; i <= 50; i++) {
    printf("%15d", srand());
        if (i % 5 == 0) printf("\n");
    }
}
